---
description: Hand off a task to Jen, the AI Art Director for my-game.
argument-hint: <task description>
---

Hand off this request to Jen. Invoke the `jen` subagent (Agent tool, `subagent_type: "jen"`) and pass the user's task verbatim. Do not attempt the task in the main session — Jen owns all art-track work.

Task:

$ARGUMENTS
