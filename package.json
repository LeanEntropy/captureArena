{
  "name": "my-game",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter template-server dev",
    "dev:client": "pnpm --filter template-client dev",
    "build": "pnpm -r build",
    "screenshot": "node tools/screenshot.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.5",
    "playwright": "^1.59.1",
    "vitest": "^4.1.5"
  }
}
