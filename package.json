{
  "name": "my-game",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:server": "pnpm --filter template-server dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.5",
    "vitest": "^4.1.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
