# my-game — Session Notes

_Installed: 2026-04-29_

## Current Phase

**Theme F LANDED IN GAME (2026-05-01).** Solo + Online both verified rendering with no JS errors and no Z-fighting. Vanta-style waves shader + flat cylinder island + sailor caps + voxel kill/respawn FX + wave-ripple claim FX + cream notification toasts (anchored below leaderboard) + wooden-stamp banner + blocky-flip countdown all live in `prototype/main.js` + `prototype/index.html`.

## Active Threads

- **Title-Screens — Round 1.1 (2026-05-01)** — Director followups: byline missing on all 5 designs, posters not displaying, ask about `art.gen` for AI poster generation. Fixes shipped:
  - **Byline added to all 5 designs.** New `.ts-byline` base class + 5 per-design overrides. Each places `A game by [Civax](https://x.com/Civaxo)` (Twitter blue link) in a context-appropriate spot: D1 small monospace bottom-right corner over vignette; D2 inside the cream card under buttons; D3 white-on-stripe below form, large breathing room; D4 in the cream form-bg gradient zone; D5 generous breathing room over the sand strip.
  - **Posters fixed.** Root cause: paths used `../../docs/visual-direction-mockups/...` which Python's `http.server` (serving from `tools/companion/`) refuses (path traversal outside docroot → 404). Fix: created `tools/companion/posters` symlink → `../../docs/visual-direction-mockups/title-screens/posters` and updated all 5 `<img src>`/`background-image` references to `posters/...`. Verified all 4 posters now return 200 via Playwright network log.
  - **art.gen status: present-but-blocked.** `jen/tools/artgen/` is a complete RunComfy CLI (`python -m tools.artgen gen --prompt ...`). Loads `RUNCOMFY_TOKEN` from `jen/.env` or `host_root/.env` or `docs/data_from_director.txt`. None set, so PIL composites kept as fallback. Generation note on companion page now explicitly tells Director "drop the token in `jen/.env` and Jen will re-run."
  - 🟡 Director still owes the design pick (currently leaning toward Design 3 / Faction Stripes per latest feedback) + the `RUNCOMFY_TOKEN`.

- **Title-Screens — Round 1 SHIPPED (2026-05-01)** — Director request: design a proper entrance page for the renamed "Land Capture" game with ~5 design directions, optionally including ThreeJS island/water animation, plus action-movie posters. Deliverables shipped (mockup-only, zero `prototype/` touch):
  - `tools/companion/title-screens.html` + `tools/companion/title-screens.js` — 5 full-screen 16:9 design mocks with the actual name+Solo+Online primitives styled in-direction:
    - **Design 1 — Cinematic Poster** (static, ~0ms/frame): Theme F overview vignetted, Bebas Neue 124px knockout title, faction-stripe edges, yellow Solo + blue Online buttons.
    - **Design 2 — Live Atoll** (live ThreeJS, ~0.5ms/frame): Vanta water shader ported 1:1 from `prototype/main.js` + cylinder island + cube character with sailor cap idle-bobbing & cycling through faction colors every 4s + 3 distant atolls + cream "driftwood" Lilita One form card. Most faithful to Director's request.
    - **Design 3 — Faction Stripes** (live ThreeJS, ~0.3ms/frame): 5 vertical color stripes; shared canvas hosts 5 rotating cube characters w/ sailor caps (one per stripe). Bowlby One SC 132px white knockout title spans all stripes.
    - **Design 4 — Voxel Block Type** (live ThreeJS, ~0.7ms/frame): "LAND CAPTURE" rendered as ~330 BoxGeometry voxel cubes via 5x7 procedural pixel font, colored by faction palette. Theme F sky gradient bg. Most "authored" expression of the BoxGeometry-only ART_ETHOS.
    - **Design 5 — Minimal Arcade** (static, ~0ms/frame): cream paper, huge bold Lilita One 168px title, SVG island silhouette with 5 faction-colored cube chars on the rim, arcade-button form. Highest readability, lowest cost.
  - `docs/visual-direction-mockups/title-screens/posters/` — 4 PIL-composited action-movie posters at 1200×1500:
    - `poster_a_five_factions.png` "FIVE FACTIONS. ONE ISLAND." — Theme F overview wide shot, vignetted, faction-key.
    - `poster_b_dominate_the_tide.png` "DOMINATE THE TIDE" — hero character + diagonal green slash. Sequel-poster framing.
    - `poster_c_last_one_standing.png` "LAST ONE STANDING" — desaturated lone-survivor framing + blue faction slash. Horror-film tension.
    - `poster_d_paint_the_world.png` "PAINT THE WORLD" — color-explosion starburst + voxel cube avatars w/ sailor caps. Pure brand.
  - **RunComfy generation note**: `RUNCOMFY_TOKEN` not set in `jen/.env` and local ComfyUI on port 8000 not reachable from this WSL session. Fallback path used: PIL composition of existing Theme F game screenshots with vignettes, cinema color grades, faction-stripe motifs, procedural typography. Generator script: `jen/scripts/generate_title_posters.py`. Re-runnable when token is provided.
  - `docs/visual-direction-plan.md` Section 20 (~150 lines): full per-design spec, perf estimates, recommendation (Jen prefers Design 2 → 5 → 4), 5 open Director questions.
  - `tools/companion/index.html` — added NEW card linking to title-screens page (green border).
  - `tools/companion/shared/nav.js` — added title-screens entry as the FIRST nav link, marked NEW.
  - **Verification (Playwright MCP)**: 0 console errors, 0 warnings (only cosmetic favicon 404). All 3 live ThreeJS canvases render with healthy WebGL contexts at 1152×648. Screenshots: `docs/visual-direction-mockups/title-screens/screens/r1-fullpage.png` + `r1-design-{1..5}-{slug}.png`.
  - **Did NOT touch**: `prototype/*` (parallel subagent's water-fix territory), `tools/companion/visual-direction.{html,js}` (parallel subagent's companion-water-sync territory), `tools/companion/{character-design,index}.html` beyond adding the NEW card. Files I own: only `tools/companion/title-screens.{html,js}`, `tools/companion/shared/nav.js` (added 1 line), `tools/companion/index.html` (added 1 card), `docs/visual-direction-plan.md` (appended §20), `docs/visual-direction-mockups/title-screens/`, `jen/scripts/generate_title_posters.py`.
  - 🟡 Director input needed on §20.6 questions (5 of them): design pick, mobile fallback for Design 2's water shader, smooth-fade transition, AI-gen poster retry, tagline lock.

- **Companion water synced to game (2026-05-01)** — Commit `2806edb`. Director asked for the companion (Theme F + E panels) to match the in-game faceted Vanta look that landed in `e7e6b56`. Ported the shader 1:1 from `prototype/main.js` into `tools/companion/visual-direction.js` `_buildWaterAndIsland()` — replaced both vertex + fragment shaders, dropped the old `uSeaColor/uFoamColor/uSunColor/uSunDir/uFoamWidth` uniforms, replaced with `uDeepColor/uLightColor/uLightDir/uWaveAmp/uFreqX/uFreqY/uFog*`. Hardcoded the Vanta palette (`#0d3556` / `#5093d0`) — the faceted look is the direction's identity, not a per-theme color. Spatial frequencies and amplitude are scaled with the `66.89/16` ARENA_RADIUS ratio so each grid cell carries the same slope at companion scale (~4.18× freq up, ~4.18× amp down) — facet density reads identically to the in-game panel, not a stretched zoom. Three.js r170 ships WebGL2 by default so `dFdx/dFdy` are core; no `extensions: { derivatives: true }` declaration needed. Single shader path serves both directions, parameterized only by `isFlatCylinder` for water Y position (-0.4 vs -0.25). Verified via Playwright at 1500×1100 — both Theme F and Theme E now render hard-edged blue triangle facets with strong dark/light contrast, zero JS errors. Screenshots: `jen-companion-themef-faceted-water.png`, `jen-companion-themee-faceted-water.png`. Also fixed stale prose in Theme E panel-body (was "vertex sin waves + foam ring + sun-glitter" → now "faceted low-poly Vanta sea (deep + light blue triangles, hard-edged Lambert per face)"). Did NOT touch parallel subagent's `tools/companion/title-screens.html`/`title-screens.js` (new files), `tools/companion/index.html`, `tools/companion/shared/nav.js`, or `prototype/portals.js` (audit subagent's territory).

- **Water visual fix — flat-shaded faceted Vanta look (2026-05-01)** — Commit `e7e6b56`. Director called out that previous Vanta port was "wrong visual" — it used smooth depth-shaded sea + scrolling sun-glitter stripes. Correct Vanta look is LOW-POLY FACETED: each triangle a distinct uniform color, hard edges. Fix in `prototype/main.js` water ShaderMaterial: kept Vanta vertex displacement formula intact, replaced fragment shader to compute per-FACE normal via `dFdx/dFdy` on view-space position (gives flat normal even with shared-vertex plane — the Vanta trick). Two-tone Lambert mix between deep blue `#0d3556` and light blue `#5093d0` with `smoothstep(0.55, 1.0, light)` contrast curve so adjacent facets read clearly different. Wave amplitude bumped 0.45 → 1.6 because the 80×80 grid (~5u cells) needed steeper triangle slopes for facet normals to vary. Removed: glitter stripes, depth-based sea gradient. Kept: discard guard inside `uIslandRadius` (no waves on island), fog. Cheaper than before (one fewer mix + no sin/dot/smoothstep glitter math). Tests: 41/41 pass. Verified via Playwright at low-angle side view (`jen-water-faceted-07-side-view-paused.png`, `jen-water-faceted-08-closeup-comparable-to-reference.png`) — clear hard-edged faceted triangles with strong dark/light contrast matching the Director's reference (`/mnt/c/.../chrome_MJdomDkhDr.png`). Zero JS errors. Logged learning: `vanta-faceted-water-glsl`.

- **Theme F SHIPPED in-game (2026-05-01)** — Director's full Theme F spec landed in solo + online via two-file change (`prototype/main.js` +~430 lines, `prototype/index.html` +~40 lines CSS):
  - **Y stratification** (Director's HARD RULE): water Y=-2.0, cylinder bottom Y=-3.0, cylinder TOP Y=0.0, territory mesh Y=0.05 with `polygonOffset: -1, -1`, trail Y=0.10. Five distinct Y planes; territory-on-cylinder approach (the "structurally cleaner" alternative the Director called out). Verified at grazing camera angles — no Z-fighting flicker.
  - **Vanta Waves shader (Path B)**: extracted Vanta's CPU vertex wave formula from `vanta.waves.min.js` (`y = oy + pow(sin(t * x * z + phase) + 1, 2) / 4 * waveHeight`, MIT) and ported it 1:1 to a GLSL vertex shader on a 80×80 PlaneGeometry. Same wave cadence, same crest shape, but runs on GPU instead of CPU (~0.3ms vs 1-2ms). Fragment shader adds depth-shaded sea color + sun-glitter stripes scrolling along a fixed direction + manual fog (since ShaderMaterial doesn't auto-pick scene.fog). Critical safety: `if (dist < uIslandRadius) discard;` in the fragment shader kills any wave fragment inside the playable arena, so animations CANNOT bleed onto the island.
  - **Cylinder island**: flat `THREE.CylinderGeometry(ARENA_RADIUS, ARENA_RADIUS, 3.0, 64)` with multi-material (sand sides + grass top + dark bottom). Top face at Y=0.0, base at Y=-3.0. The territory texture renders at +0.05 above the top — the Director's spec for distinct planes.
  - **14 cliff rocks** along the rim, sized 1.7-3.3 units (chunky-but-not-overwhelming vs character body 1.0). **3 distant atolls** sized 8-14 units, fading into fog (near=80, far=260).
  - **Sailor caps** added to `Character._buildChar`: faction-color band cube + white top cube. Pure boxes per ART_ETHOS principle 1.
  - **Lighting retune**: warm-dawn ambient `0xfff0d8` + golden-hour directional `0xfff2c8` + cool sea-bounce fill `0x88B8D0`. Sky gradient (peach top → pale teal bottom).
  - **JenFXManager**: encapsulated FX class with `triggerClaim` (3 wave-ripple rings + 8 droplet cubes), `triggerKill` (12 voxel debris cubes with bounce + faction-color tint), `triggerRespawn` (group rises Y from baseY-1.5 to baseY over 0.5s + 5 dust puffs), `triggerWin` (30 cubes drop from Y=8-12 in winning color). Single update loop disposes meshes when life >= maxLife — bounded mem.
  - **JenHUDManager**: `push()` for A's cream notification cards (Fredoka, faction-color border), `showBanner()` for D's wooden stamp (Lilita One, dark-bordered yellow). Notification stack anchored DYNAMICALLY just below `#player-leaderboard.getBoundingClientRect().bottom + 8` per frame so it never overlaps the leaderboard regardless of player count. Countdown intensification: `<= 9s` adds `.intense` class which triggers `cd-pulse` keyframes.
  - **Sim hooks wired**: `sim.onClaim` → `fx.triggerClaim` + player-only "Claimed for {Faction}" notification; `sim.onKill` → `fx.triggerKill` + "You died — killed by X" / "You killed X" notifications; respawn detected via `wasAlive` map → `fx.triggerRespawn` + "You spawned" notification; faction-state polling each frame → endangered/eliminated/recovered banners (deduped by key); match-ended once-shot → `fx.triggerWin` + "{FACTION} WINS" banner.
  - **D blocky-flip countdown** restyles existing `#hud-tl` via CSS in index.html — kept the existing `UIManager._updateTimer` HTML output, added a wood-block container around it. The element gets `.intense` class added/removed by the HUD manager.
  - **Solo verification (Playwright)**: title screen renders, game starts cleanly, all 30 chars spawn, water animates (uTime ticks), cylinder island clearly distinct from territory at all camera angles tested, sailor caps visible on every character, cliff rocks read at correct game scale, distant atolls visible in fog, FX particles dispose cleanly. Screenshots: `jen-themef-04-overview-locked.png` (overview from Y=130), `jen-themef-13-all-overlays.png` (banner + 3 notifications + intense countdown all visible), `jen-themef-14-grazing-angle.png` (NO Z-fight at side view), `jen-themef-15-rocks-resized.png` (final rocked-down version), `jen-themef-17-character-closeup.png` (sailor cap visible on green char's head).
  - **Online verification (Playwright)**: zero JS errors. Game initialized with 30 characters, water shader active, FX manager active, HUD manager active. Screenshot: `jen-themef-16-online-initial.png` (player on cylinder edge with water + island + cliff rock).
  - **Tests**: 41/41 pass before and after. Renderer-only changes; sim/server untouched.
  - **Perf note**: headless Playwright reports ~8 FPS due to no GPU acceleration (frame min 8.9ms = 112 FPS in fast path; max 174ms is the 1024² territory texture rebuild every 100ms — pre-existing). Real-Chrome perf needs Director verification.
  - **Files changed**: `prototype/main.js`, `prototype/index.html`. Renderer-only + CSS-only.
  - **Did NOT touch**: `prototype/sim/*` (subagent's diagnostic logger work in `Simulation.js`), `tools/companion/*` (parallel Jen subagent's Theme F mockup work), `server/*`, `packages/*`.

- **Slice A SHIPPED-then-REVERTED (2026-04-30)** — Slice A had Z-fight (territory plane Y=0.19 vs grass dome Y=0.18, both within 0.01 of each other) and waves animation overshooting onto the island. Reverted in `4f353bf`. Lessons baked into Theme F: never co-locate Y planes; always discard fragments inside the playable area.

- **Direction F — Atoll Hybrid mockup SHIPPED (2026-04-30)** — Director's hybrid request after Slice A revert. Built mockup-only (no game-side touch).
  - `tools/companion/visual-direction.js`: added `DIRECTIONS.F` config (~50 lines), branched `_buildWaterAndIsland()` on new `outerGround.cylinder` flag → flat single CylinderGeometry (radius=ARENA_RADIUS, height=0.6) instead of E's sand+grass dome stack. Top face IS the grass surface (CylinderGeometry multi-material `[sandy_sides, grass_top, dark_base]`) — no separate plane, structurally cleanest possible.
  - **3 distinct Y planes** (Director's hard rule from the failed Slice A): water at Y=−0.4, cylinder top at Y=+0.6, territory mesh at Y=+0.65 (territoryOffset bumped from 0.01→0.05 for F). Trail sits above territory at +0.07. FX rings at +0.07. Bounce floor `floorY = (_islandTopY || 0) + 0.1` so voxel debris bounces ON the cylinder top, not through it. Build-up respawn made baseY-aware so character rises from `baseY - 1.5` to `baseY`, not from Y=−1.5 to Y=0.
  - **Water-overlap fix:** widened `uIslandRadius` from `ARENA_RADIUS * 1.02` → `1.04` and foam width from 1.2 → 1.5 so waves never visually creep onto playable area.
  - **SceneOverlay `_buildLeaderboardGhost`:** new method renders 4 dashed placeholder rectangles (FACTIONS top-right / LEADERBOARD top-right under factions / MINIMAP bottom-left / PLAYER STATS bottom-center) when `config.showLeaderboardGhost === true`. Lets the Director visually verify the spatial constraint "notifications BELOW the leaderboard, not on top of any other UI element."
  - **Notification anchor:** new `notificationAnchor: "below-leaderboard"` config key. `_build` reads it and positions the notif stack at `top:146px right:16px` (10px top + 32px factions + 8px gap + 88px leaderboard + 8px gap), width 200px to match a real-game leaderboard column.
  - **FX dispatch in F:** claim=E's wave-ripple, kill=D's voxel-debris, respawn=D's build-up, notif=A's card-warm, banner=D's stamp, countdown=D's blocky-flip, win=D's voxel-rain. Sailor caps from E kept (Director said optional, Jen kept them — they stitch F to E and have zero perf cost).
  - HTML: added F's panel as a full-width row below E (`grid-column: 1/-1`, height 520px). Updated FX matrix to 6 columns. Updated page intro + `<h2>` from "Four" to "Six". Updated `index.html` card text.
  - Plan doc: appended Section 19 (~150 lines). 6 sub-sections: §19.1 geometry/Y planes, §19.2 per-feature spec, §19.3 config flags introduced, §19.4 game-side porting notes (speculative), §19.5 verification screenshots, §19.6 5 open questions for Director.
  - **Verification:** Playwright pass at `http://127.0.0.1:7891/tools/companion/visual-direction.html` 1500×1100. Zero errors (only favicon 404). 5 screenshots saved at `docs/visual-direction-mockups/r3/`:
    - `r3-f-baseline.png` — F at rest, all elements visible
    - `r3-f-notif-anchored-below-leaderboard.png` — 3 cream cards below the leaderboard ghost
    - `r3-f-voxel-rain-win.png` — D's voxel rain mid-fall, cubes bouncing on island top
    - `r3-e-regression-after-f.png` — E unchanged
    - `r3-fullpage-with-f.png` — all 6 panels
  - **Perf:** within budget. Same +0.7ms as E (water shader is the long pole). F adds 4 dashed DIVs (negligible) + 1 flat CylinderGeometry (~64 verts, cheaper than E's two-cylinder stack).
  - **Constraints respected:** game-side untouched. Only `tools/companion/`, `docs/visual-direction-plan.md`, `docs/visual-direction-mockups/r3/` written.
  - 🟡 Director input needed on §19.6 questions (5 of them — most importantly: "is F locked as the future game-side direction?").

- **Slice A SHIPPED — Direction E base look (2026-04-30)** [LATER REVERTED — Z-fighting + wave overlap, see F above] — Commit `b9e4c59` lands the Castaway Atoll visual identity in the actual game:
  - `prototype/sim/faction.js`: `FACTION_COLORS` retuned to `[0xE74A3F, 0x3D6CD0, 0x52B856, 0xFFCF2A, 0xA94BBE]` (cooler desaturated Blue, boosted Yellow). Palette flows into mesh tint + minimap + HUD + territory texture via the single constant.
  - `prototype/main.js`: replaced background/ground/lights with E stack — animated water shader (two summed sin waves + foam ring + scrolling sun-glitter), sand cylinder + grass top + 14 cliff-rocks rim + 3 distant atolls, warm-dawn ambient + golden-hour directional + cool sea-bounce fill, sky gradient + sea fog. Added `_islandTopY = 0.19` constant used by territory mesh, character group, trail mesh, and camera lookAt to keep everything on the grass top (not below the water). Added universal sailor cap (white top + faction-color band) to `Character._buildChar`. Wave/foam shader frequencies divided by ~6 vs mockup so wavelengths read at game scale (ARENA_RADIUS=66.89 vs mockup 10). Cliff-rock and atoll absolute dims multiplied ×6.689 so silhouettes read at distance. Foam-ring width bumped 1.2u → 5u for the same reason.
  - **Verification:** Playwright pass at `http://localhost:2567/` with name "AtollTester". Game starts cleanly (only favicon 404 console error), all 30 chars spawn across 5 factions, water uniform ticks, player can move and chase, faction palette + caps + cliff rocks + sand + foam + sun-glitter + sky gradient all render. Overview screenshot from camera (40, 110, 100) → (0, 0, 0) saved as `jen-slice-a-overview.png` matches `docs/visual-direction-mockups/r3/r3-e-baseline-viewport.png` conceptually.
  - **Tests:** all 41 still pass (`pnpm test`).
  - **Perf delta:** within budget. Water shader is the long pole at ~0.4ms; everything else <0.1ms.
  - **Constraints respected:** did NOT touch `prototype/sim/Simulation.js` (subagent has pending diagnostic logger work there). `Simulation.js` and `portals.js` had pre-existing unstaged modifications when I started — left them unstaged so the subagent's logger commit owns them. Renderer-only + 1-line palette change.
  - **Open issues for Slice B–F awareness:**
    - Territory texture has visible diagonal striping at oblique camera angles (NearestFilter rasterization). Pre-existing. Per plan §18.5 Q1, recommend keeping NearestFilter (chunky aesthetic fits cliff-rocks + caps); flagged for Director confirmation.
    - Character mesh assumption for Slice C/B: `Character.group` Y is now baseY (=`_islandTopY`); body=0.5, head=1.3, cap-band=1.72, cap-top=1.85 are all relative to that. Slice C idle-bob and walk-wobble should write to `body.position.y` and `body.rotation.x/z` (NOT `group.position.y` — that's reserved for the island-lift).
    - Slice B respawn FX: `wash-ashore` should set `group.position.y = baseY - 1.2` and tween up to `baseY` over 0.6s. baseY = `this.baseY` (Character field).
    - Slice B claim/kill FX entry-point Y should be `this._islandTopY + 0.06` for the territory mesh surface (don't write Y=0).

- **Visual Direction Plan — Round 4: Execution Plan (2026-04-30)** — Director picked E + dropped Trail outline. Built Section 18 in `docs/visual-direction-plan.md` (lines 663–920, +260 lines):
  - **18.1** — Tier 0–4 with #4 dropped (23 items remain, each with target file in `prototype/`, effort, perf cost, deps).
  - **18.2** — Per-feature breakdown of E's actual-game landing: 16 features, each with mockup source range → target file → integration notes. Key gotcha flagged: ARENA_RADIUS in mockup=10 vs game=66.89 — geometry tied to ARENA_RADIUS scales fine, but absolute-unit sizes (cliff-rock 0.5–1.0u, atoll widths) need re-scaling at game scale.
  - **18.3** — 6 execution slices (A: island look 8h, B: moments feel right 8h, C: characters alive 4h, D: HUD pass 8h, E: match-end moment 8h, F: polish optional 4h). Total core ≈ 36h matching round-3 estimate.
  - **18.4** — Conflicts with in-flight subagent a674740c (Simulation.js claim debug) flagged as low-risk — only 1 sim file touched (`faction.js` palette constant). Trail-gap fix `2f5ed2c` and BFS algo `3c6438b` verified non-conflicting.
  - **18.5** — 6 open questions for Director (drop AA item #9, wordmark rename, sailor cap variants, mobile water fallback, flag persistence, lock faction-blue palette).
  - 🟡 Director input still needed: pick a slice to begin (recommend slice A — biggest visual delta for 1-day spend).

- **Visual Direction Plan — Round 3 (2026-04-30)** — Director feedback on round 2: "at least one suggestion should be of the background as water or sea and the arena as an island." Round 3 adds Direction E — Castaway Atoll. Deliverables shipped:
  - `docs/visual-direction-plan.md` — appended Section 16 (Direction E full spec) + Section 17 (round 3 open questions). E uses procedural ShaderMaterial water (~0.4ms vertex+fragment, 80×80 plane, two summed sin waves + foam ring + sun-glitter), grass-on-sand cylindrical island (sand cylinder Y=-0.05 r1.18→1.08, grass top Y=+0.18 at r=ARENA_RADIUS), 14 cube cliff-rocks scattered around the rim, 3 distant atolls in the fog. Faction palette retuned for sea-readability (Blue desaturated cooler, Yellow boosted vs teal). Sailor cap (white cube + faction-color band) on every character. Per-direction FX: wave-ripple claim, splash kill, wash-ashore respawn, driftwood notifications, sail banner, sundial countdown, tide-rise win (3 foam-ring sweeps + faction flag rises on a wooden pole at center as a permanent victory marker + DOM seagulls).
  - `tools/companion/visual-direction.html` + `visual-direction.js` — added Direction E config, water+island builder, all per-direction FX, 5th panel as full-width row at bottom of grid. JS now ~2700 lines (added ~600 lines for E). FX matrix table extended to 5 columns.
  - `tools/companion/index.html` card updated to "Round 3" with E summary.
  - All 10 trigger buttons (claim/kill/respawn/notif/endangered/eliminated/recovered/countdown/win/reset) verified clickable + renders cleanly via Playwright (no JS errors, only favicon 404).
  - Verification screenshots saved at `docs/visual-direction-mockups/r3/`:
    - `r3-fullpage.png` — full companion page with all 5 directions
    - `r3-e-baseline-viewport.png` — E in resting state (water glittering, sailor caps, cliff-rocks, distant atoll)
    - `r3-e-kill-fx.png` — kill FX mid-flight
    - `r3-e-win-flag-up.png` — victory flag fully raised on the wooden pole at center
    - `r3-e-static-overlays.png` — sundial + 3 driftwood notifications + sail banner all visible together
  - Performance: +0.7ms over Direction D baseline. Within 16ms budget. Mobile fallback path noted (swap shader for static gradient texture, drops to ~0.2ms).
  - 🟡 Director input still needed: direction pick (now A/B/C/D/E or hybrid) + round-2 questions (audio, mobile, lore) + 6 new round-3 questions (cull or keep all 5, sea/island as a layout overlay for non-E directions, mobile water cost, faction-blue palette commit, flag persistence, cap variant strategy).
  - **Note:** my round-3 staged changes were bundled into commit `eb6a092` ("fix: leaderboard scroll wheel + max-height interaction") by a parallel session — they're in HEAD but the commit history is muddled. Future round-3 reverts would need cherry-picking those file paths only. Commits before the bundling: `2af41f7` (leaderboard wiring) and `125f5fc` (round 1+2 art).

- **Visual Direction Plan — Round 2 (2026-04-30)** — Director feedback on round 1: directions felt too similar (same trigger claim across all 4, only 2 distinct kill FX). Round 2 fixes via end-to-end visual differentiation. Deliverables shipped:
  - `docs/visual-direction-plan.md` — appended Round 2 sections 7–15: per-direction claim FX, kill FX, respawn FX, notification system spec, faction-state banner spec, countdown spec, game-win FX, distinct character/trail/minimap treatment, round-2 open questions, updated effort estimates.
  - `tools/companion/visual-direction.html` + `visual-direction.js` — fully reworked. Each scene now has 8 trigger buttons per direction (Claim, Kill, Respawn, Notify ×3, Endangered banner, Eliminated banner, Recovered banner, Countdown toggle, Game Win, Reset). New `SceneOverlay` class wraps each canvas with a DOM layer that hosts notifications, faction banners, countdown widget, and DOM win-FX (confetti / grid sweep / watercolor wash). Per-direction FX dispatch via `config.{claimFlashStyle, killBurstStyle, respawnStyle, notificationStyle, bannerStyle, countdownStyle, winStyle}` keys. ~1300 lines.
  - Companion index card updated to reflect Round 2.
  - All 40 trigger buttons (4 directions × 10 actions) verified clickable without throwing via Playwright.
  - Verification screenshots saved at `docs/visual-direction-mockups/r2/` (`r2-fullpage.png`, `r2-kill-fx-comparison.png`, `r2-win-fx-comparison.png`, `r2-claim-notif-countdown.png`).
  - Per-direction FX summary:
    - **A (Sunset)**: claim sunburst rays, kill confetti, respawn sunrise halo+beam, win 3D fireworks + DOM confetti. Cream/coral HUD, Fredoka.
    - **B (Neon)**: claim shockwave + strobe, kill implode, respawn glitch-in scanline, win 3 expanding rings + grid sweep. Dark/cyan HUD, Space Mono terminal-prefix notifications.
    - **C (Painted)**: claim ink-bleed + splotches, kill petals, respawn ink-drop bloom, win arena-wide watercolor wash. Parchment/dashed HUD, Caveat Brush.
    - **D (Voxel)**: claim stamp-wave + dust puff, kill voxel debris, respawn build-up from below + dust, win voxel rain. Dark wood HUD, Lilita One block lettering.
  - 🟡 Director input still needed on direction pick + audio + mobile + lore + title-screen direction (round 1 questions). Round 2 adds 6 new questions.

- **Visual Direction Plan — Round 1 (2026-04-30)** — Director asked for full visual analysis: prioritized add-list (kill/death/respawn FX, claim feedback, background treatment, character look) plus several art-direction options with mockups. Deliverables shipped:
  - `docs/visual-direction-plan.md` — full markdown plan: 24 prioritized items across 5 tiers, 4 art directions (A: Sunset Arcade, B: Neon Pulse, C: Painted Plains, D: Voxel Plate), comparison matrix, recommendation, 5 open questions for Director.
  - `tools/companion/visual-direction.html` + `visual-direction.js` — live mockup page rendering all 4 directions as actual Three.js scenes with the real cube character mesh, real territory texture (canvas + nearest-filter, dithered for D), real lighting model, and clickable VFX triggers (Trigger Claim / Trigger Kill on each panel). 5 WebGLRenderers (well under 16 context limit). Verified renders cleanly via Playwright; full-page screenshot at `docs/visual-direction-mockups/companion-fullpage.png`.
  - Companion nav + index card updated.
  - Recommendation: Ship Tier 0 (claim flash, kill burst, death polish, trail outline, HUD pop) regardless of direction. Primary: D (Voxel Plate) — lowest cost, lowest risk, maximum expression of box-only constraint. Safety: A (Sunset Arcade). Cosmetic mode: B (Neon Pulse).
  - 🟡 Director input needed: direction pick + audio scope + mobile target + lore for "what is the arena sitting on" + title screen direction.
  - ComfyUI not reachable from WSL session at task time (port 8000/8188 not responding); pivoted to live-WebGL mockups instead of static AI-gen images. Live scenes are arguably more useful here since the directions hinge on lighting/animation/VFX behavior, not still-frame composition.

- **Character visual design v2** — Companion page rebuilt with live interactive Three.js scenes (was 2D canvas). All 8 sections now render real BoxGeometry characters with auto-rotation and drag interaction. Shared `createCharacter(options)` function ready for game reuse. Awaiting Director approval to implement in PlayerRenderer.ts.
  - **2026-04-29 fix #1:** Resolved white-container rendering bug. Root cause: 35 individual WebGLRenderers exceeded browser limit (~16 WebGL contexts). Replaced with single shared WebGLRenderer + 2D canvas blit pattern.
  - **2026-04-29 fix #2:** Complete rendering rewrite to render-to-image approach. The blit pattern still caused context-loss errors (constant setSize 35x/frame, missing preserveDrawingBuffer). New approach: ONE WebGLRenderer renders each scene to PNG data URLs at init (24 rotation frames per scene, 16 for thumbnails), then disposes the WebGL context entirely. Result: 0 WebGL contexts at runtime, zero errors/warnings. Drag-to-rotate and auto-rotation work via pre-rendered frame cycling (setInterval + IntersectionObserver for off-screen pause). Async init with requestAnimationFrame yields between sections prevents GPU shader compilation overflow.

## Resolved Questions

- Character shape: **BOXES ONLY**. Director explicitly rejected rounded/capsule forms. Paper.io 2 / Crossy Road style.
- Box aesthetic is the permanent direction, not a placeholder.

## Open Questions

- Team mode color selection: will teams pick from the existing 8-color palette, or get dedicated team colors?
- Yellow (#FFEB3B) has low contrast on white ground. Amber (#F9A825) proposed as replacement.
- Which proportion archetype to use as default? (Chunky recommended, but Director may prefer Chibi or Squat.)

## Next Steps

- [ ] Director reviews character-design-v2.html recommendations
- [ ] Implement approved box character changes in PlayerRenderer.ts
- [ ] Add player position rendering to Minimap.ts
- [ ] Begin UI/HUD art direction pass (name entry, death screen, leaderboard styling)
